/** @type {import('next').NextConfig} */
const nextConfig = {
  output: 'export', // enable static exports for GHub pages
  basePath: '/typicode-next-app', // my repo name
  reactStrictMode: true,
};

export default nextConfig;
